# ─── Rentify Admin Panel (Vite) ───────────────────────────────────────────────
# Copy to .env and fill in real values.

# Backend API URL — must point to the /api/auth/admin prefix
VITE_API_URL=http://localhost:5000/api/auth/admin
